[Unit]
Description=gunicorn daemon
After=network.target

[Service]
Restart=always
WorkingDirectory=/root/VM-deployer
ExecStart=/usr/local/bin/gunicorn -k eventlet --workers 1 --threads 400 -b 0.0.0.0:5000 wsgi:application

[Install]
WantedBy=multi-user.target
